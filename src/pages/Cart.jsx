import React from 'react'
import { useSelector } from 'react-redux'

const Cart = () => {
  const {cart} =useSelector((state)=>{})
  return (
    <div>
      Cart
    </div>
  )
}

export default Cart
